# Optional: Default timezone when no timezone is passed to the tool.
# Use IANA timezone names: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# Examples: America/New_York, Europe/London, Asia/Tokyo, Asia/Kolkata
# DEFAULT_TIMEZONE=Asia/Kolkata

# Optional: Require this token for all requests (Bearer header or ?token= query param).
# When set, unauthenticated requests return 401. Use a long random string.
# MCP_AUTH_TOKEN=your-secret-token-here
